comment: 'This script returns an HTML result of the cloud providers in the incident.
  The result will be displayed in the following font colors: AWS - red, GCP - green,
  Azure - blue.'
commonfields:
  id: CortexXDRCloudProviderWidget
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.0.0
    itemVersion: 6.0.4
    packID: CortexXDR
    packName: Cortex XDR by Palo Alto Networks
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    toServerVersion: ""
dockerimage: demisto/python3:3.10.13.80014
engineinfo: {}
mainengineinfo: {}
name: CortexXDRCloudProviderWidget
nativeimage:
- "8.3"
- "8.4"
pswd: ""
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('CortexXDRCloudProviderWidget', 'start', __line__())
  ### pack version: 6.0.4


  COLORS = {'AWS': 'ff0000',
            'GCP': '339966',
            'Azure': '0000ff'}

  HTML_START = """<h1 style="color: #2e6c80; height: 170px; line-height: 170px; text-align: center;">"""
  HTML = """<span style="color: #{color};"><strong>{provider}&nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; </strong></span>"""


  ''' COMMAND FUNCTION '''


  def get_cloudprovider_html_result():
      cloud_providers = get_cloud_providers()
      results = [HTML_START]
      results.extend([HTML.format(provider=provider, color=COLORS.get(provider, '000000')) for provider in cloud_providers])

      html_result = ''.join(results)
      return {'ContentsFormat': EntryFormat.HTML,
              'Type': EntryType.NOTE,
              'Contents': html_result}


  def get_cloud_providers():
      incident = demisto.incident()
      xdr_alerts = demisto.get(incident, 'CustomFields.xdralerts')
      if not xdr_alerts:
          raise DemistoException('xdralerts is not configured in the incident')
      if not isinstance(xdr_alerts, list):
          xdr_alerts = [xdr_alerts]
      cloud_providers = {alert.get('cloudprovider') for alert in xdr_alerts}
      return cloud_providers


  ''' MAIN FUNCTION '''


  def main():
      try:
          return_results(get_cloudprovider_html_result())
      except Exception as ex:
          return_error(f'Failed to execute CloudProviderWidget. Error: {str(ex)}')


  ''' ENTRY POINT '''

  if __name__ in ('__main__', '__builtin__', 'builtins'):
      main()

  register_module_line('CortexXDRCloudProviderWidget', 'end', __line__())
scripttarget: 0
subtype: python3
system: true
tags:
- dynamic-section
type: python
